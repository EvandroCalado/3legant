import { Meta } from '@storybook/react';
import ReviewItem from '.';

export default {
  title: 'Components/ReviewItem',
  component: ReviewItem,
} as Meta;

export const Default = () => <ReviewItem />;
